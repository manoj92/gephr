import { AppRegistry } from 'react-native';
import App from './App';

// Register the main component for React Native
// Use 'main' as the component name for Expo
AppRegistry.registerComponent('main', () => App);
